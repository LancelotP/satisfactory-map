{
  "name": "satisfactory-map",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "10.15.3",
    "yarn": "1.13.0"
  },
  "scripts": {
    "postinstall": "cd api/build && yarn install --production=false",
    "heroku-postbuild": "cd api/build && yarn build"
  }
}
