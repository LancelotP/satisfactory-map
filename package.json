{
  "name": "satisfactory-map",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "10.15.3",
    "yarn": "1.13.0"
  },
  "cacheDirectories": [
    "packages/api/node_modules"
  ],
  "scripts": {
    "heroku-postbuild": "cd packages/web && yarn install --production=false && yarn build",
    "start": "cd packages/web && yarn start"
  }
}
