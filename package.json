{
  "name": "satisfactory-map",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "10.15.3",
    "yarn": "1.13.0"
  },
  "scripts": {
    "build": "cd packages/api && yarn build",
    "postinstall": "cd package/api && yarn install --production=false"
  }
}
