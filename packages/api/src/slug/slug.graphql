type Slug implements IMarker {
  id: ID!
  map: Map!

  lat: Float!
  lng: Float!

  createdAt: DateTime!
  updatedAt: DateTime!

  type: SlugType!
  addedBy: User
}

enum SlugType {
  GREEN
  YELLOW
  PURPLE
}
